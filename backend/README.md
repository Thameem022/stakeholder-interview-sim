# stakeholder-interview-sim — backend

FastAPI service powering the realtime interview loop and evaluation endpoints.

See repo root README for full setup. Quick dev:

```bash
cd backend
uv sync
uv run uvicorn app.main:app --reload
```
